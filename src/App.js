import React, { Component } from 'react';
import Routes from './Routes';


// App component
class App extends Component {
  render() {
    // add routes instead
    return (<Routes />
    );
  }
}

export default (App);
